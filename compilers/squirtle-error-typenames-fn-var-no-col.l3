//test error_parse
// test that the functions and variable names cannot collide
// with the typenames

typedef int blah;

int blah(int x) {
    while(x > 0) {
        x--;
    }
    return x;
}


int main() {
    int r = 0;
    return blah(3);
}
